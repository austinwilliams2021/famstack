{
  "devDependencies": {
    "turbo": "^1.10.1"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build && npm start",
    "start": "node apps/api/dist/main"
  },
  "workspaces": [
    "apps/*"
  ]
}
